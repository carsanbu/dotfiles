- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.config/tmux/: ../unix/tmux/
    ~/.config/tmuxp/: ../unix/tmuxp/
    ~/.config/:
      glob: true
      path: ../.config/*
      relink: true
    ~/.aliases.sh: ../unix/scripts/aliases.sh
